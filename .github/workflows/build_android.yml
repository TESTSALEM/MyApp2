name: Kivy Android Build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install System Dependencies 🏗️
      # تثبيت متطلبات نظام Linux الأساسية لـ P4A
      run: |
        sudo apt update
        sudo apt install -y git zip unzip openjdk-17-jdk python3-pip autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev libblas-dev liblapack-dev libssl-dev build-essential lld

    - name: Prepare Python Environment
      run: |
        pip install --upgrade pip setuptools wheel
        pip install --upgrade buildozer cython 

    - name: Manual Android SDK License Acceptance and Setup 🔑
      # تم تغيير المسار إلى $HOME/android-sdk لتجنب مشكلة الصلاحيات
      run: |
        export ANDROID_HOME=$HOME/android-sdk
        mkdir -p $ANDROID_HOME/cmdline-tools
        
        # تحميل أداة sdkmanager
        wget -q https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip -O android-cmdline-tools.zip
        unzip -q android-cmdline-tools.zip -d $ANDROID_HOME/cmdline-tools/latest
        
        # إضافة المسار إلى PATH
        export PATH=$PATH:$ANDROID_HOME/cmdline-tools/latest/bin
        
        # قبول جميع التراخيص بشكل إجباري
        yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses
        
        # تثبيت المكونات المطلوبة لبناء Kivy
        $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager "platform-tools" "platforms;android-32" "build-tools;33.0.2"

    - name: Force Buildozer Clean 🧹
      # التنظيف لضمان أن Buildozer سيبدأ في استخدام المكونات المثبتة يدوياً
      run: |
        buildozer android clean

    - name: Final Build Kivy App 🚀
      # أمر البناء الفعلي
      run: |
        buildozer android debug

    - name: Upload APK Artifact
      uses: actions/upload-artifact@v4
      with:
        name: Android-APK-Release
        path: bin/*.apk
        
